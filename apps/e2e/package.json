{
  "name": "@hop-and-barley/e2e",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test:e2e": "pnpm --filter @hop-and-barley/auth-contract build && pnpm --filter @hop-and-barley/api-client build && playwright test",
    "test:e2e:ui": "pnpm --filter @hop-and-barley/api-client build && playwright test --ui",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.13.0",
    "@playwright/test": "1.62.1",
    "@types/node": "26.2.0",
    "typescript": "5.9.3"
  }
}
